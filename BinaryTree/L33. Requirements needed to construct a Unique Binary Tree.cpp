Note:
1. We need Inorder & either pre/post order to construct an unique Binary tree.
2. Using pre/post order we will get the root & using the root we can 
   use inorder traversal to construct the unique binary tree.